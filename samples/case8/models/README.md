# Models

This directory is for generated model artifacts:

- `*.onnx`: exported YOLO models.
- `*.om`: ATC-converted Ascend offline models.
- `*_labels.txt`: class labels generated during export.
- `*_metadata.json`: input/output metadata generated during export.

These files are not source code and are not intended to be committed.


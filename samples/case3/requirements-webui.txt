# Complete Python dependency set for MIDI-DDSP Studio.
numpy>=1.22,<2
mido>=1.3,<2
python-rtmidi>=1.5,<2
sounddevice>=0.4,<1

fastapi>=0.115,<1
pydantic>=2.5,<3
uvicorn>=0.32,<1
websockets>=12,<16

# `acl` is supplied by CANN/PyACL and `ais_bench` by the existing Ascend
# benchmark environment; neither is installed from public PyPI here.

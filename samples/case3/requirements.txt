# Python dependencies required by the Ascend board runtime and Web UI.

# Numeric runtime used by DDSP synthesis and audio processing.
numpy>=1.22,<2
scipy>=1.8,<2

# MIDI file/message API. Required for .mid analysis, rendering and playback.
mido>=1.3,<2

# Mido backend for physical USB MIDI input ports. The installed package is
# named python-rtmidi, while the Python import name is rtmidi.
python-rtmidi>=1.5,<2

# Audio output.
sounddevice>=0.4,<1

# Web service runtime.
fastapi>=0.115,<1
pydantic>=2.5,<3
uvicorn>=0.32,<1
websockets>=12,<16

# `acl` is supplied by CANN/PyACL and `ais_bench` by the existing Ascend
# benchmark environment; neither is installed from public PyPI here.

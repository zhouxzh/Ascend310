# Qwen2.5 1.5B 8T 内存门与正确 CANN 复测记录

_记录日期：2026-09-04｜批次：`qwen25-15-memory-gate-20260904-rerun`｜板卡：原 Ascend310B4/8T，当前地址 `192.168.11.14`_

## 结论

Qwen2.5-1.5B-Instruct 的完整七个文件已在隔离目录中下载并通过字节数、SHA-256
校验。首个受控加载命令随后发生 `SIGSEGV`（退出码 `139`），但复核发现该命令把
`set_env.sh` 生成的 CANN `PYTHONPATH` 覆盖掉了；其 `te/hccl` 缺失警告使这次崩溃
只能作为**环境受污染的诊断证据**，不能单独证明模型与 310B 不兼容。必须使用保留
CANN 路径的全新批次复测后，才能给出最终加载结论。

因此本记录（首个批次）的状态为：

```text
artifact_verified
environment_verified
model_load: blocked (SIGSEGV 139; PYTHONPATH 污染，需复测)
generation: not-run
json/sse/API: not-run
quality/performance: not-run
admission: blocked
```

这不是“中文质量失败”，也不是可复用的 NPU 推理结果。不能把 Qwen2.5-1.5B 接入
8090、7867/7868 或正式的 8080、7861、7865 链路。

`192.168.11.14` 是原来 `.90`/`.178` 地址对应的同一块 8T 板；IP 变化不产生新的
硬件性能批次。

> **复测边界：** 本文件保留失败原始证据，但不能把它提升为最终硬件阻断。修正后的
> 命令必须验证 `acl`、`te`、`hccl` 都能从 CANN 路径发现，并在独立时间戳目录写入
> 新的报告；在此之前注册表应保持 `blocked/diagnostic-contaminated`，不得启动服务。

## 工件和环境

来源为 [Qwen/Qwen2.5-1.5B-Instruct](https://huggingface.co/Qwen/Qwen2.5-1.5B-Instruct)，
固定 revision：`989aa7980e4cf806f80c7fef2b1adb7bc71aa306`。

| 文件 | bytes | SHA-256 | 状态 |
| --- | ---: | --- | --- |
| `model.safetensors` | `3087467144` | `dd924a11b4c220f385b51ffa522daea7c9f3d850e31b162bb5661df483c6d3ee` | 已校验 |
| `tokenizer.json` | `7031645` | `c0382117ea329cdf097041132f6d735924b697924d6f6fc3945713e96ce87539` | 已校验 |
| `config.json` | `660` | `98d2ff8cc47488d08a2b0b3acf4eb99ef210779b42bd48605f6b8e36acdbf670` | 已校验 |
| `tokenizer_config.json` | `7305` | `5b5d4f65d0acd3b2d56a35b56d374a36cbc1c8fa5cf3b3febbbfabf22f359583` | 已校验 |
| `generation_config.json` | `242` | `e558847a8b4402616f1273797b015104dc266fe4b520056fca88823ba8f8ebe6` | 已校验 |
| `merges.txt` | `1671839` | `599bab54075088774b1733fde865d5bd747cbcc7a547c5bc12610e874e26f5e3` | 已校验 |
| `vocab.json` | `2776833` | `ca10d7e9fb3ed18575dd1e277a2579c16d108e32f27439684afa0e10b1440910` | 已校验 |

板端运行时（没有安装、升级或删除包）：

| 项目 | 实测值 |
| --- | --- |
| 主机/架构 | `orangepiaipro` / `aarch64` |
| SoC | `Ascend310B4` / 8T |
| CANN / `npu-smi` | `8.0.0` / `25.2.0` |
| Python | `3.9.2` (`/usr/local/miniconda3/bin/python`) |
| MindSpore / MindNLP | `2.4.10` / `0.4.1`，均来自 `/home/HwHiAiUser/.local` |
| 系统内存 | 约 15 GiB，总可用约 13 GiB；无 swap |
| NPU 基线 | `npu-smi` 为 `8172 / 15610 MB` used；MindSpore allocator 另报可分配空间约 `3972 MB` |
| NPU 诊断 | `Health: Alarm`、HugePages `15/15`；Alarm 仅记录，不单独判失败 |

板端 `base` 是 dirty-base，已有 Torch/Torch-NPU 等包保持原样。为确保使用已验证的
MindSpore/MindNLP，执行时显式 `unset PYTHONNOUSERSITE`；设置为 `1` 会隐藏用户 site
中的 MindNLP，不能作为本批次环境。

## 执行和原始证据

模型放在板端隔离根：

```text
/home/HwHiAiUser/case9-qwen25-15-test-20260904/
└── artifacts/models/qwen2.5-1.5b-instruct/
```

使用了进程级 watchdog、离线变量和单 token 上限；没有启动 HTTP 服务。**该命令的
`PYTHONPATH` 覆盖缺陷如下，不能作为正确复现命令：**

```bash
source /usr/local/miniconda3/etc/profile.d/conda.sh
conda activate base
source /usr/local/Ascend/ascend-toolkit/set_env.sh
unset PYTHONNOUSERSITE
export HF_HUB_OFFLINE=1
export TRANSFORMERS_OFFLINE=1
export PYTHONPATH=/home/HwHiAiUser/case9-qwen25-15-test-20260904
timeout --signal=TERM --kill-after=15s 300s \
  python scripts/qwen25_15_memory_gate.py \
  --profile qwen2.5-1.5b-mindspore --max-tokens 1
```

正确复测时必须保留 CANN 路径：

```bash
export PYTHONPATH=/home/HwHiAiUser/case9-qwen25-15-test-20260904:${PYTHONPATH:-}
```

当前脚本默认启用严格 placement 门（`CASE9_REQUIRE_PLACEMENT_EVIDENCE=1`），不会
读取环境中遗留的放宽设置。只有明确要复现 context-only 诊断时才追加
`--context-only`；该选项永远不能产生可晋级结果：

```bash
timeout --signal=TERM --kill-after=15s 300s \
  python scripts/qwen25_15_memory_gate.py \
  --profile qwen2.5-1.5b-mindspore --max-tokens 1 --context-only
```

第二次运行的关键日志：

```text
MindSpore do not support bfloat16 dtype, we will automaticlly convert to float16
Free memory size is less than half of total memory size
Device HBM total size: 16368312320
Device HBM free size: 3971985408
timeout: the monitored command dumped core
exit_code=139
```

这里的两个内存数字来自不同层次：`npu-smi Memory-Usage` 是设备侧总体记账，
`Device HBM free size` 是 MindSpore/运行时当时可分配的池空间，不能相减、互换或
当作同一指标的前后快照。`dmesg` 记录了进程绑定设备后退出和 `trs_proc_release ... Res leak`；退出后
`npu-smi` 仍约为 `8173 / 15610 MB`，没有可归因的 Case9 用户进程可以安全停止。
未知的系统日志守护进程没有被杀死，也没有执行设备重置。

该进程收到的是原生 `SIGSEGV`，不是 Python 异常。因而 watchdog 看到 core dump
后，Python 的 `finally`、原子 JSON 报告写入和 provider 清理器都不能被宣称已经执行；
下面列出的 `npu-after-followup.txt` 等是外部跟进采集的快照，只能证明崩溃后的观察状态，
不能证明 ACL/MindSpore 资源已完整释放。

原始文件（本地 Git 忽略复现目录，均来自板端）：

```text
repro/mindspore-chat-20260904/reports/board8t/
  qwen2.5-1.5b-mindspore/qwen25-15-memory-gate-20260904-rerun/run.log
  qwen2.5-1.5b-mindspore/qwen25-15-memory-gate-20260904-rerun/npu-after-followup.txt
  qwen2.5-1.5b-mindspore/qwen25-15-memory-gate-20260904-rerun/dmesg-tail-followup.txt
  qwen2.5-1.5b-mindspore/qwen25-15-memory-gate-20260904-rerun/processes-followup.txt
  qwen2.5-1.5b-mindspore/qwen25-15-memory-gate-20260904-rerun/memory-followup.txt
  qwen2.5-1.5b-mindspore/qwen25-15-memory-gate-20260904-rerun/disk-followup.txt
  qwen2.5-1.5b-mindspore/qwen25-15-memory-gate-20260904-rerun/environment-followup.txt
```

## 门禁解释

| 门 | 状态 | 说明 |
| --- | --- | --- |
| G0 环境 | `environment_verified` | SoC、CANN、Python、MindSpore/MindNLP 和资源快照已记录；环境仍是 dirty-base |
| G1 工件 | `artifact_verified` | 七个文件的大小和 SHA-256 全部匹配固定 revision |
| G2 加载/NPU | `blocked` | 初始化阶段 `SIGSEGV`；没有合法的模型实例或 placement 证据 |
| G3 JSON/SSE | `not-run` | 未启动 worker/API |
| G4 长输出/G5 稳定性 | `not-run` | 没有生成请求 |
| G6 中文/英文质量 | `not-run` | 无输出可评分 |
| G7 性能 | `not-run` | 不得从失败过程计算 token/s |
| G8 候选链 | `not-run` | 正式和候选端口均未修改 |

首次尝试因隔离根漏同步 tokenizer/config 文件而在 G1 被阻断；该同步操作错误与模型
兼容性分开保留，补齐七个文件后才得到本报告中的 `SIGSEGV` 结果。

## 后续边界

注册表暂以 `blocked` 保存本批次的受污染诊断，20T/B1 仍为独立 `not-run`。只有在
明确的设备恢复/内存预算决策后，才可以另开新批次；新批次必须重新记录基线，不能在
当前占用状态下反复加载。不得为绕过本失败安装 Torch、Torch-NPU、vLLM、MindIE、
新版 MindSpore/CANN 或切换 CPU/云端。

当前可继续使用的候选仍以各自逐板证据为准：Qwen1.5、TinyLlama、DeepSeek 的历史
结果不能由本次 1.5B 失败推断；Qwen3 在当前 MindNLP 0.4.1 没有 loader。正式入口
`8080 -> 7861 -> 7865` 保持不变。

## 正确 CANN 环境复测（v2）

在发现首批命令覆盖 `PYTHONPATH` 后，使用独立批次
`qwen25-15-memory-gate-20260904-cann-fixed-v2` 重新执行。此次先保留
`set_env.sh` 的 CANN 路径，并由脚本实际 import `acl`、`te`、`hccl`，同时校验
`npu-smi` 的目标 SoC。

| 门 | 实测结果 |
| --- | --- |
| CANN 模块 | `acl.so`、`te`、`hccl` 均 available/imported；`PYTHONPATH` 正确保留 |
| SoC | expected/actual `Ascend310B4`，当前 IP `192.168.11.14` |
| 工件 | 7/7 文件字节数和 SHA-256 通过 |
| 模型加载 | 通过，`63.426752 s` |
| 单 token 生成 | 通过，`16.159310 s`，输出 `我是`，prompt `36` tokens |
| 其余门 | placement、JSON/SSE、长输出、稳定性、质量、性能均 `not-run` |

该结果证明在正确 CANN 环境下可以创建模型并完成一次 NPU 生成，修正了首批
`SIGSEGV` 证据的环境污染问题；它仍不是稳定聊天服务验收。provider 的
`placement_status` 仍为 `context_only`，报告 `promotion_eligible=false`，
因此 Profile 继续保持 `blocked`，不启动任何 Case9 HTTP 入口。

本批次原始证据（均已同步到本地忽略目录）：

```text
repro/mindspore-chat-20260904/reports/board8t/qwen2.5-1.5b-mindspore/
  qwen25-15-memory-gate-20260904-cann-fixed-v2/report.json
  qwen25-15-memory-gate-20260904-cann-fixed-v2/run.log
  qwen25-15-memory-gate-20260904-cann-fixed-v2/exit-code.txt
  qwen25-15-memory-gate-20260904-cann-fixed-v2/npu-before.txt
  qwen25-15-memory-gate-20260904-cann-fixed-v2/npu-after.txt
  qwen25-15-memory-gate-20260904-cann-fixed-v2/dmesg-tail.txt
  qwen25-15-memory-gate-20260904-cann-fixed-v2/processes-after.txt
  qwen25-15-memory-gate-20260904-cann-fixed-v2/meminfo-after.txt
  qwen25-15-memory-gate-20260904-cann-fixed-v2/pythonpath.txt
```

v2 证据文件的本地 SHA-256：

```text
report.json         0cd6c3647f2fb6bb92824375203d6babb8e12b64bfe1f00a894feae787188f17
run.log             e96190c58b95082316c1aead9263aef37e655c8fa456485efe67f7d4c2fdfb8a
exit-code.txt       bde294368bfed77c2cddf8cec271d398aee9cdbab3b26e1059281bd33adb0120
npu-before.txt      807a314dd514c50e21baae3ece26089c0718fa237da7988047204147e263d20f
npu-after.txt       fa6c114e69591956f4bbd268e965635c918266dd135ba5393ef090a874254ab
dmesg-tail.txt      3bc6c69bd9f29b77e301670bd968c7e8880e10c92ec01df4b69abbe3908b66aa
processes-after.txt ec4a46f4100e2f4ab9f3630cf714661a117f746f224a0a65732cccd8498aeb46
meminfo-after.txt   ee556653c710d141cbbc80d0bcc5300bcbd709b3f59783305b444d87214597c8
pythonpath.txt      524f54a524d08ba3242a48d458b510add792cde9e16d621cc721bd75794331f5
```

v2 运行中 `npu-smi` 的 `Memory-Usage` 从约 `14961/15610 MB` 变化到
`14929/15610 MB`；该字段与 MindSpore allocator 的可用池不是同一口径。

## 正确 CANN 环境复测（v3b，连续两个 token）

为确认 v2 的单 token 不是偶然的初始化结果，在同一块原 8T 板、同一固定工件和
保留 CANN `PYTHONPATH` 的 shell 中执行了独立批次
`qwen25-15-memory-gate-20260904-cann-fixed-v3b`。命令使用进程级 watchdog、离线
工件、`--max-tokens 2`，没有启动 HTTP 服务，也没有修改正式端口。

| 门 | 实测结果 |
| --- | --- |
| CANN 模块/SoC | `acl`、`te`、`hccl` 均可发现并实际导入；expected/actual `Ascend310B4` |
| 工件 | 7/7 文件大小和 SHA-256 通过 |
| 模型加载 | 通过，`58.393638 s` |
| 连续生成 | 通过，`16.883210 s`；prompt `36` tokens，completion `2`，文本 `我是Q`，`finish_reason=length` |
| 清理后 NPU | `npu-smi` `14913/15610 MB`（before cleanup），进程退出后外部快照 `2506/15610 MB` |
| 其余门 | placement、JSON/SSE、长输出、稳定性、质量、性能仍 `not-run` |

当前脚本若重现该结果，报告状态为 `diagnostic_passed`，按 gate 约定退出码为 `3`（表示
诊断成功但不可晋级，不是服务验收成功）。本地保存的 v2/v3b 原始 `exit-code.txt`
分别记录旧脚本的 `0`/`0`；该历史值不改写为新退出码。v3b 报告中的 provider
`placement_status` 仍为 `context_only`，
故 Profile 继续 `blocked`，不启动候选或正式 HTTP 链路。

v3b 原始证据已同步到本地忽略复现目录：

```text
repro/mindspore-chat-20260904/reports/board8t/qwen2.5-1.5b-mindspore/
  qwen25-15-memory-gate-20260904-cann-fixed-v3b/report.json
  qwen25-15-memory-gate-20260904-cann-fixed-v3b/run.log
  qwen25-15-memory-gate-20260904-cann-fixed-v3b/exit-code.txt
  qwen25-15-memory-gate-20260904-cann-fixed-v3b/npu-before.txt
  qwen25-15-memory-gate-20260904-cann-fixed-v3b/npu-after.txt
```

其中 v2 `report.json` 的本地 SHA-256 为
`0cd6c3647f2fb6bb92824375203d6babb8e12b64bfe1f00a894feae787188f17`；v3b 文件哈希以
同步目录中的原始清单为准。v2/v3b 只证明在正确 CANN 环境下模型可以被 MindSpore
加载并完成极短 NPU 生成，不证明稳定聊天、中文质量或生产准入。

## 正确 CANN 环境复测（v4，当前首选诊断证据）

v2/v3b 之后又在同一块原 8T 板、当前地址 `192.168.11.14` 上执行了批次
`qwen25-15-memory-gate-20260904-cann-fixed-v4`。本批次同步了当前脚本、Profile
注册表和 provider，保留 `set_env.sh` 注入的 CANN `PYTHONPATH`，并显式使用
`--context-only --max-tokens 2`。它仍是非服务诊断，未占用或修改 `8080/7861/7865`
及候选 HTTP 入口。

| 门 | 实测结果 |
| --- | --- |
| CANN 模块/SoC | `acl`、`te`、`hccl` 均可发现并实际导入；expected/actual `Ascend310B4` |
| 工件 | 7/7 文件大小和 SHA-256 通过；模型 SHA `dd924a11b4c220f385b51ffa522daea7c9f3d850e31b162bb5661df483c6d3ee` |
| 模型加载 | 通过，`53.068432 s` |
| 连续生成 | 通过，`23.169714 s`；prompt `36` tokens，completion `2`，文本 `我是Q`，`finish_reason=length` |
| 运行期资源 | 进程 RSS peak `8,369,860,608` bytes；生成前 NPU `5532/15610 MB`，进程内 after 快照 `14984/15610 MB` |
| 清理后外部快照 | 进程退出后 `npu-after.txt` 为 `2516/15610 MB`，SoC 仍为 `Ascend310B4` |
| 诊断状态 | `diagnostic_passed`；脚本退出码 `3`，表示诊断成功但不可晋级 |
| 其余门 | 严格 placement、JSON/SSE、长输出、稳定性、质量、性能仍 `not-run` |

v4 的本地原始证据和逐文件 SHA-256 清单位于 Git 忽略复现目录：

```text
repro/mindspore-chat-20260904/reports/board8t/qwen2.5-1.5b-mindspore/
  qwen25-15-memory-gate-20260904-cann-fixed-v4/report.json
  qwen25-15-memory-gate-20260904-cann-fixed-v4/run.log
  qwen25-15-memory-gate-20260904-cann-fixed-v4/exit-code.txt
  qwen25-15-memory-gate-20260904-cann-fixed-v4/npu-before.txt
  qwen25-15-memory-gate-20260904-cann-fixed-v4/npu-after.txt
  qwen25-15-memory-gate-20260904-cann-fixed-v4/SHA256SUMS.txt
```

关键文件 SHA-256：

```text
report.json    f34c7f0da1d54daa103e79dd70a4ebdace3ea763b9437c77b2ebc83e7265808a
run.log        b76467150b172a5b7656775d899ed3541d7eaed38e7f64e71acffd4a667102d6
exit-code.txt  1121cfccd5913f0a63fec40a6ffd44ea64f9dc135c66634ba001d10bcf4302a2
npu-before.txt 2e5d1306c2122e710842c9a80d37752a087179fced967024d0ac63762f33fa7b
npu-after.txt  b595a8db0faaccef8a56c666fa80c26f21d64bc760c90f665f09c181e0f97723
```

这份 v4 是当前注册表引用的首选正确 CANN 证据，但 `context-only` 明确不满足
placement 门；因此 Profile 仍为 `blocked`，不能启动 MindSpore worker，也不能把
一次短生成当作聊天机器人或 XiaoZhi 后端验收。IP 从 `.90/.178` 变为 `.11.14`
只改变连接地址，不改变硬件身份或既有性能批次。

## 重启后的最终受控诊断（2026-09-04）

用户随后重启了同一块 8T 板（当前 `192.168.11.14`，重启时间本地
`2026-09-04 17:52:25`）。在保留 CANN `PYTHONPATH`、离线工件和 `context-only` 边界的
条件下重新执行了单 token 诊断，批次为
`20260904T100745Z-qwen25-15-reboot`。七个文件、`acl/te/hccl` 导入和
`Ascend310B4` 身份均通过，模型加载也通过（`57.870395 s`），但生成阶段发生真实的
设备/运行时分配失败：

```text
ProviderUnavailable: MindSpore generation failed: Alloc failed, size:481482240
mindspore/ops/kernel/ascend/pyboost/aclnn_utils.h:184 MemBlock
```

NPU 记账从约 `7703/15610 MB` 增至 `14993/15610 MB`，进程 RSS 峰值约
`9583710208` bytes，可用主机内存约 `1933279232` bytes；报告内部退出码为 `1`。
这次不是首轮 `PYTHONPATH` 覆盖造成的假失败，而是正确 CANN 路径下的运行期内存门失败。
`placement_status=context_only` 且 `promotion_eligible=false`，没有启动服务或 API。

重启后原始证据已同步到：

```text
repro/mindspore-chat-20260904/reports/board8t/qwen2.5-1.5b-mindspore/
  20260904T100745Z-qwen25-15-reboot/report.json
  20260904T100745Z-qwen25-15-reboot/run.log
  20260904T100745Z-qwen25-15-reboot/exit-code.txt
```

`report.json` SHA-256 为
`03d16bb1903288bbdc206c99255fdf0f1cd795186a52898acabae0805a0bc9e7`，`run.log` SHA-256
为 `bf3123a48b7f494919ade38722c1510da8e40a3e922fe8a31475e49d11fb0d4b`，退出码旁证
`exit-code.txt` SHA-256 为
`4355a46b19d348dc2f57c046f8ef63d4538ebb936000f3c9ee954a27460dd865`。进程退出后只读
跟进快照 `environment/post-qwen25-15-reboot-20260904T101948Z/` 显示 NPU 已回落到
`3005/15610 MB`，但 `dmesg-lpm-fault-tail.txt` 仍记录周期性 `DRV_LPM_FAULT 0x80E3A203`。

因此本文件的最终判定为：工件和环境门通过，1.5B 的加载/生成诊断在当前 8T 配置下被
运行期内存失败阻断；Profile 继续 `blocked`，不进行长输出、稳定性、中文质量、性能或
候选链测试。任何后续尝试都必须在再次确认设备恢复后另开批次，并先重新采集基线；不得
通过降低记录标准、CPU fallback、Torch 或其他框架绕过该门。

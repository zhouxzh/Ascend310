# Qwen2.5 1.5B 8T 预检记录

_记录日期：2026-09-04｜批次：`qwen25-15-preflight-8t-20260904`｜板卡：原 Ascend310B4/8T，当前地址 `192.168.11.14`_

> **状态说明：** 本文记录的是完整权重同步前的只读预检，已被后续的
> [受控内存门批次](37-qwen25-1.5b-8t-memory-gate-20260904.md)取代。后续批次已
> 同步并核验完整 `model.safetensors`，但模型初始化以 `SIGSEGV`/退出码 `139`
> 失败；因此本文件中的 `not-run` 只适用于预检当时，不能覆盖后续的 `blocked` 结论。

## 结论

本批次完成了 Qwen2.5-1.5B-Instruct 的固定工件登记、配置/tokenizer 下载校验和
MindNLP 架构级 loader 预检。当时没有下载约 3.09 GB 的模型权重，没有创建模型实例，
也没有启动服务或修改正式端口；完整权重及其失败的加载尝试另记于 docs/37。

当时的结论为 `not-run`，不是硬件失败；在后续完整权重批次完成后，当前 Profile 的
8T 最终状态已更新为 `blocked`：

- HF revision 已固定为 `989aa7980e4cf806f80c7fef2b1adb7bc71aa306`；
- `config.json` 声明 `model_type=qwen2`、`architectures=Qwen2ForCausalLM`，当前
  MindNLP `0.4.1` 的 `Qwen2Config` 可以解析该配置；
- 当前板约有 15.6 GB NPU 显存、约 15 GiB 系统内存且无 swap。完整权重为
  `3,087,467,144` bytes；同板 Qwen2.5-0.5B 已出现约 91% NPU 内存峰值，因此不能
  把 1.5B 直接加载当作安全的下一步；
- 后续受控内存门已经执行，结果和原始日志见 [docs/37](37-qwen25-1.5b-8t-memory-gate-20260904.md)。
- 在当前占用状态下不要重复加载；任何新尝试都必须建立独立批次并重新采集基线。

`192.168.11.14` 是原来使用过的 `.90`/`.178` 地址对应的同一块 8T 板，IP 变化不产生
新的硬件性能批次。

## 工件锁

来源为 [Qwen/Qwen2.5-1.5B-Instruct](https://huggingface.co/Qwen/Qwen2.5-1.5B-Instruct)，
本批次所有 URL 都指向上述不可变 commit。SHA-256 和大小如下：

| 文件 | bytes | SHA-256 | 本批次状态 |
| --- | ---: | --- | --- |
| `model.safetensors` | `3087467144` | `dd924a11b4c220f385b51ffa522daea7c9f3d850e31b162bb5661df483c6d3ee` | 预检时只登记；后续已下载并核验（见 docs/37） |
| `tokenizer.json` | `7031645` | `c0382117ea329cdf097041132f6d735924b697924d6f6fc3945713e96ce87539` | 已下载并核验 |
| `config.json` | `660` | `98d2ff8cc47488d08a2b0b3acf4eb99ef210779b42bd48605f6b8e36acdbf670` | 已下载并核验 |
| `tokenizer_config.json` | `7305` | `5b5d4f65d0acd3b2d56a35b56d374a36cbc1c8fa5cf3b3febbbfabf22f359583` | 已下载并核验 |
| `generation_config.json` | `242` | `e558847a8b4402616f1273797b015104dc266fe4b520056fca88823ba8f8ebe6` | 已下载并核验 |
| `merges.txt` | `1671839` | `599bab54075088774b1733fde865d5bd747cbcc7a547c5bc12610e874e26f5e3` | 已下载并核验 |
| `vocab.json` | `2776833` | `ca10d7e9fb3ed18575dd1e277a2579c16d108e32f27439684afa0e10b1440910` | 已下载并核验 |

板端缓存目录：

```text
/home/HwHiAiUser/case9-mindspore-chat/artifacts/models/qwen2.5-1.5b-instruct
```

文件传输遵循 `.part`、SHA-256 校验、原子改名；未创建任何模型权重的临时文件。

## 板端环境

命令在同一 shell 中执行：

```bash
source /usr/local/miniconda3/etc/profile.d/conda.sh
conda activate base
source /usr/local/Ascend/ascend-toolkit/set_env.sh
```

| 项目 | 实测值 |
| --- | --- |
| 主机/架构 | `orangepiaipro` / `aarch64` |
| SoC | `Ascend310B4` / 8T |
| CANN / `npu-smi` | `8.0.0` / `25.2.0` |
| NPU 内存 | `2276 / 15610 MB`（预检时） |
| 系统内存 | 总计约 `15 GiB`，可用约 `13 GiB` |
| Swap | `0 B` |
| MindSpore / MindNLP | `2.4.10` / `0.4.1` |
| `mindnlp.transformers.models.qwen2` | 存在 |
| `Qwen2ForCausalLM` | 存在 |
| `mindnlp.transformers.models.qwen2_5` | 不存在（Qwen2.5 在此版本使用 Qwen2 架构名） |

板端已有 `torch`/`torch_npu`/`torchaudio` 等 dirty-base 包保持原样；本适配没有导入或
安装这些包。`npu-smi` 的 `Health: Alarm` 只作为诊断字段记录。

## 配置和 loader 预检

在不分配权重的情况下运行：

```python
import json
from pathlib import Path
from mindnlp import transformers as t

root = Path("/home/HwHiAiUser/case9-mindspore-chat/artifacts/models/qwen2.5-1.5b-instruct")
config = json.loads((root / "config.json").read_text())
config_class = t.Qwen2Config
parsed = config_class.from_dict(config)
```

输出摘要：

```text
config_model_type=qwen2
config_arch=['Qwen2ForCausalLM']
config_loaded=True
layers=28 hidden=1536
mindnlp=0.4.1
```

这只证明配置和类名可以被当前 loader 解析，不证明权重加载、Ascend placement、ACL
执行、API 或中文质量。

## 门禁状态

| 门 | 状态 | 证据边界 |
| --- | --- | --- |
| G0 环境 | `environment_verified`（预检） | 8T/CANN/MindSpore/NPU 可见；共享 base 仍是 dirty-base |
| G1 工件 | `not-run` | 小文件已核验，但完整权重尚未下载，因此不能称为完整 artifact gate |
| G2 加载/NPU | `not-run` | 未创建模型实例，未执行权重加载 |
| G3 JSON/SSE | `not-run` | 未启动 8090 worker |
| G4 长输出/G5 稳定性 | `not-run` | 没有生成请求 |
| G6 质量/G7 性能/G8 候选链 | `not-run` | 依赖后续完整加载和 API 门 |

预检完成时注册表 `configs/chat_model_profiles.json` 的 `Ascend310B4` 行保持 `not-run`；
后续完整权重批次已将该行更新为 `blocked`，20T/B1 行仍然是独立的 `not-run`，不能由
8T 预检或失败推断。

## 下一步边界

后续边界已经完成如下：

1. `model.safetensors` 已在隔离目录中完成长度和 SHA-256 校验；
2. 模型加载前后采集了 NPU/RSS，并使用了进程级超时；
3. 初始化阶段收到 HBM 可用空间警告后发生 `SIGSEGV`，批次停止并保留日志、dmesg
   和快照；
4. 因加载失败，没有进入 JSON、SSE、长输出、稳定性、质量或性能门。

不得为绕过该失败安装 Torch、Torch-NPU、vLLM、MindIE、新版 MindSpore/CANN，或切换
CPU/云端。正式 `8080 -> 7861 -> 7865` 链路保持不变。

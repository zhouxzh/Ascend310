Primary request: Create a rigorous conceptual neural-network structure figure for the reviewed TorchSig YOLO11s wideband spectrogram detector used in a Chinese university textbook. The figure explains the exact high-level network and output contract, not a generic image classifier. Start with a 1024 by 1024 RGB time-frequency image whose horizontal axis means time and vertical axis means frequency. Show the YOLO11s backbone with stride-2 convolution stages, C3k2 blocks, SPPF, and C2PSA; show a bidirectional FPN plus PAN neck; and show three P3, P4, P5 detection feature levels at strides 8, 16, and 32. The anchor-free decoupled Detect head has a box-regression branch and a 51-score-channel output branch; do not call this a 51-class semantic branch. The output has shape [1,55,21504], where 55 equals four box values plus 51 score channels and 21504 equals 128x128 plus 64x64 plus 32x32 candidates. Place an inset after the box branch: DFL with 16 bins per coordinate, Softmax, expected coordinate, then CPU NMS grouped by the predicted score channel. Do not claim that the CPU computes network inference; CPU only runs deterministic decoding and NMS after NPU OM output.
Layout: left-to-right three bands named Backbone, Neck, and Detect head; a small lower-right output-decoding inset. Keep the three multi-scale routes visually traceable with no crossed arrows.
Components: input time-frequency image; Conv downsample; C3k2; SPPF; C2PSA; upsample; concatenate; P3/8, P4/16, P5/32; decoupled box branch; decoupled 51-score-channel branch; DFL 16 bins; output tensor; CPU NMS grouped by predicted score channel; final boxes/software-label-mapping/confidence.
Connections: input to backbone; backbone pyramid into FPN/PAN neck; neck feature maps into Detect; Detect output to DFL and class scores; joined tensor to CPU NMS; arrows are left to right.
Text (verbatim): "时频图 [1,3,1024,1024]", "Backbone", "Neck: FPN + PAN", "Detect head", "P3 / 8", "P4 / 16", "P5 / 32", "C3k2", "SPPF", "C2PSA", "框回归", "51 个输出评分通道", "DFL: 16 bins", "输出 [1,55,21504]", "4 坐标 + 51 评分", "CPU: 分类别 NMS", "检测框 / 评分 / 置信度".
Scientific constraints: this is the reviewed YOLO11s detector contract, not a claim about training accuracy or 51-class modulation-recognition accuracy. The upstream training metadata used single_cls=true even though the exported tensor has 51 score channels. The 1024x1024 image is RGB repeated from one Blackman-FFTW spectrogram. P3/P4/P5 have spatial dimensions 128x128, 64x64, and 32x32. The NPU executes the ONNX/OM neural network; the CPU only makes the model input and performs output decoding/NMS. Use visual grouping to distinguish CPU, NPU, and tensors. Do not show an FFT inside the neural network.
Color encoding: blue input/preprocessing boundary; green NPU network blocks; orange output decoding; purple CPU NMS; use colorblind-safe contrast.
Avoid: equations, pseudo-text, layer counts not verified by the reviewed artifact, generic photographic elements, gradients, shadows, 3D, watermarks, titles, figure numbers, or any claim of accuracy.
